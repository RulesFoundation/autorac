A maqaf binds the Hebrew word before it -- a prefix stack or a word of a compound -- to the word or number after it across wrap space -- spaces or one line wrap, never a blank line -- in proof matching and in numeric extraction alike, by one shared pattern ("ל־ 1⁄2", "עשרים ו־ שלושה", "עשרים ו־
שלושה", "שלושה־ רבעים"); the fractional tail after a rate follows "ו־ " as it follows "ו־"; evidence matching keeps every paragraph gap, so an excerpt quotes a blank line as a blank line, whether the line ends in "\n", "\r\n" or a bare "\r", and reads each run of whitespace once, in linear time; and the readers that run on the raw text and the context classification see line ends and that wrap space as the cleaner does, so no spelling of the space after a maqaf changes what a text states, in grounding or in the recall inventory.

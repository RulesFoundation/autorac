A maqaf binds its prefix to the word or number after it across wrap space -- spaces or one line wrap, never a blank line -- in proof matching and in numeric extraction alike ("ל־ 1⁄2", "עשרים ו־ שלושה", "עשרים ו־
שלושה"); the fractional tail after a rate follows "ו־ " as it follows "ו־"; evidence matching keeps every paragraph gap, so an excerpt quotes a blank line as a blank line, whether the line ends in "\n", "\r\n" or a bare "\r", and reads each run of whitespace once, in linear time; and the readers that run on the raw text -- the direct-percentage and money matchers, the structural reference passes -- see line ends and the wrap space after a maqaf as the cleaner does, so no spelling of that space changes what a text states, in grounding or in the recall inventory.
